# error-memory

Responsibility:

- Persist UNKNOWN and newly discovered error candidates
- Attach context clips and Omni correction
- Support human review and promotion to error catalog

Primary object:

- `error_memory` in `docs/data_schema_v2.md`
