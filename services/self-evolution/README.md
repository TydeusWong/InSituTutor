# Self-Evolution Services

This directory contains the second-stage learning pipeline.

Current scope implements the TODOlist_05 sections 0-4 foundation:

- v5 session directory conventions.
- CLI entry points for each self-evolution stage.
- `finalize_session.py` manifest generation for a recorded raw session.
- Shared path and JSON helpers in `common.py`.

The complete ASR, timeline alignment, Omni reflection, error slicing,
detector-plan generation, and strategy patch application are implemented in
the later M1-M11 tasks from `TODOlists/TODOlist_05.md`.

